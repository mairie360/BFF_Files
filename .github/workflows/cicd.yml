name: BFF Files CICD

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  CICD:
    uses: mairie360/CICD/.github/workflows/BFFs-cicd.yml@v1.15.0
    with:
      package_name: bff-files
      cicd_version: v1.13.2
      node_version: "22"
    secrets: inherit